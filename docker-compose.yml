services:
  backend:
    build: .
    ports:
      - "8000:7860"
    volumes:
      - ./data:/home/user/app/data
    env_file:
      - .env.docker
    environment:
      - ALLOWED_ORIGINS=${ALLOWED_ORIGINS}
    restart: unless-stopped
